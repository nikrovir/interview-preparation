# Вопросы на собес
- С какими версиями приходилось работать?
- В модели есть свойство $append, для каких целей оно используется?
- Чем отличается has() от whereHas()?
- Что такое и зачем Неявная привязка / Implicit binding
- Что такое scopes, какие бывают виды и для чего используются
- Жизненный цикл запроса
- Что такое Guard / Gates / Policies
- Что такое проблема N+1, как избежать? Дополнительно: в каких случаях используется with(), load(), а в каких loadMissing()?
- Как ты обрабатываешь большие выборки данных? Дополнительно: чем отличается chunk() от chunkById() и от cursor() (курсор использует генератор и держит соединение открытым, забирая по 1 записи или небольшому буферу)?
- Что такое LazyCollection?
- В чём разница между Service Container и Service Providers, для чего они нужны? Чем отличаются singleton, bind и instance в контейнере?
- Что делает метод boot() в AppServiceProvider и чем он отличается от register()?
- Как ты работаешь с очередями (Queues)? Как отлаживаешь и мониторишь?
- Как работаешь с Events & Listeners? Когда предпочитаешь Observers?
- Как выполнить операцию, не вызвав ивент? (**withoutEvents() или saveQuietly()**)
- Приходилось ли писать команды? В каких методах инициализируется di в очереди (**handle()**), ивенте (**handle()**) и команде (**handle() и __construct()**)?
- Какие оптимизации применяешь для highload-проектов (кэширование, Redis, Octane, оптимизация запросов)?
- Работал ли ты с Laravel Octane? Какие преимущества/проблемы заметил?
- Используешь ли Event Sourcing / CQRS в проектах?
- [Не совсем laravel] Что такое (Redis) Mutex и для чего используются? (Например тебе нужно написать распределённый рейтлимитер, или команду, которая единовременно выполнялась только один раз, или другая распределённая история)
- Как решить проблему гонки при конкурентных запросах (например, при бронировании)? **lockForUpdate()**, **Redis Mutex**, **Атомарные обновления через where()**, **Оптимистичные блокировки** (Можно добавить поле version или updated_at и проверять его перед обновлением)

static bind
какой принцип solid нарушают чаще всего?
какой опыт с эластиком/кафкой?
Settings::firstOrFail()->client()->associate();